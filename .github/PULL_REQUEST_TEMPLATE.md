## What changed

<!-- One or two sentences. What does this fix or add, and why? -->

## How it was tested

<!-- Commands run, hardware used (chip + board), or "sim driver only". -->

## Checklist

- [ ] `make && make bridges && make test` passes, and the tree is warning-clean.
- [ ] A test covers the new behaviour (or an existing one was extended).
- [ ] Parallel docs updated in the same change — man pages, `docs/manual.md`,
      `spec.md`, the client libraries — per [CONTRIBUTING.md](../CONTRIBUTING.md).
- [ ] Every commit is signed off (`git commit -s`) — see
      [Developer Certificate of Origin](../CONTRIBUTING.md#developer-certificate-of-origin).
