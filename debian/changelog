imud (1.10.0-1) unstable; urgency=medium

  * New driver mpu6500 for the TDK MPU-6500, the six-axis part inside the
    MPU-925x. Boards sold as MPU-9250 are frequently relabelled MPU-6500s;
    those now run as the six-axis IMU they are, with a separate
    magnetometer in [mag] for heading.  The mpu9250 and mpu9255 probes
    already refused such a board; they now name mpu6500 as the driver to
    use.
  * imud builds and runs on big-endian hosts. The binary packet and the
    .imucap file are converted field by field instead of memcpy'd in host
    order, so they stay little-endian whatever the machine, and the Debian
    packages are now Architecture: linux-any.  No wire-format change of
    its own: the bytes a little-endian host emits are identical to before.
    The packet does move to v18 in this release, for a separate change.
  * A gyro+accelerometer board with no magnetometer runs, with [mag]
    driver = "none". imud opens no magnetometer bus and claims no [mag]
    int_gpio; roll, pitch, heave, the sea-state statistics and rate of
    turn are unaffected.  Heading starts at zero and is relative to the
    orientation imud booted in, not to earth north, and drifts at the
    gyro's bias-instability rate. FLAG_MAG_VALID stays clear for the whole
    run and no declination is applied.
  * mpu6500 is no longer experimental. It is validated on real silicon by
    an imud-imutest --all report: 61 checks passed, none failed. The
    daemon no longer warns at startup when it is selected.
  * Wire format v18: the packet grows 276 to 288 bytes with flags_ext, a
    second 32-bit flag word, and eight reserved bytes. Every existing
    field keeps its offset. Its first bit says no magnetometer is
    configured.  Breaking: consumers reject a wire version they do not
    know, so deploy the daemon and its consumers together. flags had
    assigned all sixteen bits, which is what forced the new word — a
    consumer must ignore bits it does not recognise, and a later flag then
    needs no further bump. The imud-arduino client needs its own update.
  * NMEA withholds $HCHDM, $HCHDG and $HCHDT, and leaves $PASHR's heading
    field null, while no magnetometer is being fused. An uncalibrated
    magnetometer still publishes all of them.  A rig with no magnetometer,
    or one that has stopped delivering, used to publish a dead-reckoned
    heading as magnetic heading; no heading sentence has a field that
    could qualify it. Consumers now get no heading rather than a wrong
    one. $TIROT and $IIXDR are unaffected, and imud-mon shows heading as
    --.
  * The packet's fifo_overflow flag (bit 7) is now actually set when
    samples are lost, and the daemon logs the gap at 1 Hz saying whether
    the hardware FIFO overflowed or the internal ring dropped.  The bit
    has been documented since the format was first published and was never
    raised, so no consumer could detect a sample gap; only imud-status's
    counter showed one, and not when it happened. It is latched for about
    two seconds so a slow consumer still sees it.
  * mpu925x reset() now waits the MPU-925x product spec's register start-
    up time before reading the part back, and resets the gyro, accel and
    temperature signal paths as TDK's own Linux driver does for this
    generation.  The previous settle was the accelerometer's cold-start
    figure, which is shorter than the interval before a register access is
    guaranteed to answer at all.
  * The armhf packages are now built on Raspbian at the ARMv6 baseline, so
    they run on every Raspberry Pi that boots a 32-bit userland.
    Previously they were built to Debian's ARMv7 baseline.  On a Pi 1 or
    Pi Zero/Zero W the previous packages installed without complaint and
    then died on an illegal instruction, because both ports report the
    same "armhf" architecture. Those boards can now install from the apt
    repository. The arm64 packages are unchanged.

 -- Richard Simpson <richcreations@gmail.com>  Sat, 05 Sep 2026 09:31:46 -0700

imud (1.9.1-1) unstable; urgency=medium

  * An uncalibrated magnetometer is now fused for heading instead of
    ignored. Heading is offset by the uncorrected hard iron but bounded,
    where before it dead-reckoned without bound. Heading-only, so roll and
    pitch are untouched, and withdrawn automatically under gross iron.
    New flag bit 15, FLAG_MAG_UNCAL, marks the uncalibrated-but-fused
    state. FLAG_MAG_VALID is unchanged and still means calibrated and
    healthy, so existing consumers need no change. The wire packet is
    byte-identical and still v17. This spends the last free flag bit. Set
    mag_fuse_uncal = false to restore the old behaviour.  ACTION: Run
    imud-cal mag for a heading accurate enough to navigate on; this is a
    floor, not a substitute.
  * The accelerometer update no longer corrects heading or shrinks the
    attitude covariance about the gravity axis. Gravity says nothing about
    rotation about gravity, and doing either could collapse the covariance
    and report the filter converged while heading was wrong.  With no
    magnetometer aiding heading, cov[9] and FLAG_FUSION_CONVERGED now
    report an unaided heading axis honestly: the covariance grows and the
    converged flag stays clear. A consumer that treated the flag as
    permission to trust heading was previously misled in exactly that
    case.

 -- Richard Simpson <richcreations@gmail.com>  Mon, 31 Aug 2026 16:49:28 -0700

imud (1.9.0-1) unstable; urgency=medium

  * SPI transport for the IMU and the magnetometer. [imu] and [mag] each
    gain bus, spi_dev and spi_speed_hz, and choose independently.  Default
    is bus = "i2c"; existing configs are unaffected.
  * New magnetometer driver rm3100 (PNI RM3100): magneto-inductive, no
    SET/RESET coil, I2C or SPI, 1-600 Hz. Experimental until validated on
    hardware.
  * Higher sample rates: ism330dhcx and lsm6dso gain 3332 and 6664 Hz,
    icm42688p 4000 through 32000 Hz. These are rates the datasheets always
    specified and the drivers had never offered.  6664 Hz needs SPI;
    icm42688p above 8000 Hz will not run on a Raspberry Pi.
  * The manual installs as an Info page: info imud.  Building from source
    now requires texinfo.
  * Sample latency is instrumented. The [stats] line splits it into FIFO
    residence, which your fifo_wm and rate decide, and the daemon's own
    pipeline.
  * imud-status says when heading is dead-reckoned. Without a magnetometer
    calibration the filter's mag update never runs and heading drifts on
    the gyro alone, which nothing in the output used to show.
  * Startup warns when [imu] spi_speed_hz is below 2.5 MHz and a
    magnetometer shares the same SPI controller, naming the rate to use —
    below that the magnetometer can stop measuring while still answering
    the bus.
  * Sample rates are carried in milli-Hz throughout, so the advertised
    rates are now the silicon's own. The ISM330DHCX ladder is 6664/2^n,
    making its bottom rung 13.016 Hz where the datasheet prints 12.5.
    odr_hz accepts fractional rates. A rate that rounds to zero is
    refused.
  * imud --replay exits when the capture runs out, after letting the last
    samples reach the filter. It used to sit there until killed.
  * Replaying one capture twice now produces identical filter output, and
    sim_speed decides the pace rather than the reader threads' hardware
    poll cadences.
  * A reader thread that gives up stops the daemon instead of leaving it
    running with a dead sensor. A magnetometer that stops delivering is
    reported once per outage, and again when it recovers.
  * Heave and sea state accumulate in double, so a long run no longer
    loses resolution in the integrators.
  * Calibration was applied in the wrong frame when [mount] was not
    identity: the daemon rotated board to body first and applied the
    sensor-frame calibration second.  Only affects a rotated mount; with
    [mount] identity the two frames coincide.  ACTION: Re-run imud-cal for
    any sensor on a non-identity mount.
  * The daemon could not start on arm64 or armhf under its own systemd
    unit, and installing or upgrading could leave a Raspberry Pi 5 unable
    to open its GPIO chip.
  * Several sample-timestamp defects: chip_ts could run backwards between
    bursts, a corrupted read could stamp a whole burst seconds into the
    future, the timer period was trusted rather than measured, and
    timestamps carried two offsets.
  * The magnetometer ran at a third of its configured rate: the driver
    confirmed each sample by reading Meas_M_Done, but that bit is also the
    interrupt acknowledge, so a reader blocked on the edge never saw it
    set.  Only affected an installation with [mag] int_gpio set.
  * imud-cal mag could write a soft-iron scale the swing never measured,
    stretching an axis to cover data that was never taken. An axis must
    now be swept through at least 0.8 of the fitted radius, capping any
    correction at 1.25x.
  * An MPU FIFO overflow desynchronised every later read: the bytes the
    part drops to make room are never a whole number of sample-sets, and
    this FIFO carries no tag to resync from.

 -- Richard Simpson <richcreations@gmail.com>  Mon, 10 Aug 2026 04:48:20 -0700

imud (1.8.2-1) unstable; urgency=medium

  * BEHAVIOUR CHANGE: a bad value in a config file is now fatal for the
    daemon and all five bridges, where it was previously accepted with
    no message at any log level. Three classes: a value too long for its
    field was truncated and used, so a 130-character [stream] socket
    became a different, valid 107-character path that the daemon bound
    and served while every bridge and libimud client connected to the
    path in the config file and found nothing; an out-of-range integer
    wrapped, so dest_port = 4294977414 landed as port 10118 and
    odr_hz = 4294968129 as 833; and nan, inf and 1e999 loaded cleanly
    into any float key. 18 integer keys also gained semantic bounds --
    ports 1..65535, int_gpio 0..255, i2c_addr 0x00..0x7F -- because
    dest_port = 70000 fits an int and then becomes port 4464 when
    htons() truncates it.
  * BEHAVIOUR CHANGE: imud --config PATH now names one file. The
    $HOME/.config/imud/imud.conf fallback ran whether or not --config
    was given, so a mistyped path silently started the daemon on someone
    else's config, while imud(8) has documented --config as replacing
    the search path since the beginning. A missing PATH now means
    built-in defaults, with a warning. A daemon started without --config
    still searches /etc/imud/imud.conf then $HOME.
  * Fix: a non-finite calibration poisoned the filter permanently. A
    cal.json holding {"gyro":{"bias":[nan,0,0]}} loaded without
    complaint and w = gyro - bias was then non-finite on every predict
    step; q_normalize could not repair it, because NaN fails its
    n > 1e-10 test and the quaternion passed through untouched. 1000
    level, noiseless samples later the published roll, pitch, yaw and
    quaternion were still all NaN, rendered as "nan" into NMEA. Both
    loaders now reject non-finite values and name the field, imud-cal
    refuses to write one, and the MEKF sanitises its own state once per
    fusion loop as a backstop.
  * Fix: a calibration that is finite but enormous is refused too. A
    soft_iron element of 3.33e37 is finite, and soft_iron x (sample -
    hard_iron) for a sample of zero is -3.8e38, which overflows a float.
    imud-cal now checks that the arithmetic survives ordinary samples
    (probes of 0 and +/-1000, above the full scale of every supported
    sensor) rather than bounding what a calibration may physically be.
  * New flag bit FLAG_STATE_RESET (bit 14): the MEKF found a non-finite
    value in its own state and reset itself. Latched until the filter
    re-converges, since at up to 500 Hz a momentary bit is invisible to
    a 1 Hz consumer. Shown as R by imud-mon and as imud_state_reset by
    the Prometheus bridge. No wire-format change: the packet stays
    v17 / 276 bytes and a reserved bit simply gained a meaning.
  * Fix: SIGHUP reload only ever failed on a daemon started from the
    $HOME config. Reload re-read the path the daemon was asked for
    rather than the one it read, so such a daemon answered every
    systemctl reload with "config reload failed" and named no path.
    Packaged installs always have /etc/imud/imud.conf and were
    unaffected; source installs had no working hot reload at all.
  * Fix: deleting a key from the config and reloading kept the old
    value. Reload seeded from the running config rather than the
    defaults, so removing [nmea] rate_hz left the previous rate in place
    while the daemon logged "config reloaded". Reload now matches a
    restart for the hot fields. A config file that has been deleted
    still keeps the running config, deliberately unlike startup.
  * Fix: a failed output thread left its socket bound with nothing
    behind it. Listeners are bound before any thread exists, so a
    pthread_create failure left a socket that nobody would accept() from
    and a client connected into the backlog and waited forever. Losing
    the binary stream is now fatal, since the bridges and every libimud
    consumer read it and the unit is Restart=on-failure/RestartSec=3.
    The opt-in nmea, hirate and position threads still warn and carry
    on, but close what they had bound, so a client gets ECONNREFUSED.
    READY=1 now means threads running, not sockets bound.
  * Fix: imud.service forbade the gpiochip config/imud.conf asked for.
    The config shipped gpio_chip = "gpiochip4" for a Pi 5 while the unit
    allowed only /dev/gpiochip0 under DevicePolicy=closed, so no
    combination of the two shipped files was correct on any board. The
    unit now lists all four supported nodes; an allow for a node the
    board does not have is inert, so one unit serves Pi 4 and Pi 5.
  * Fix: imud-imutest accepted garbage in its numeric options. --odr and
    friends used atoi/atof, which report nothing: a bad --odr was
    silently discarded, a bad --imu-addr became 0 and took, probing I2C
    address 0, and a bad --int-gpio became 0, documented as "skips the
    interrupt check". All twelve conversions are now usage errors.

 -- Richard Simpson <richcreations@gmail.com>  Wed, 05 Aug 2026 12:00:00 -0700

imud (1.8.1-1) unstable; urgency=medium

  * Fix: imud did not start on stock Debian. Every unit hardcoded
    Group=gpio, but nothing created the gpio or i2c groups; Raspberry Pi
    OS ships them and plain bookworm/trixie ships neither, so systemd
    refused to start the unit (216/GROUP). Both groups are now created if
    absent, on every install rather than only when the user is first
    created, so an upgrade repairs an affected host. New udev rule
    etc/60-imud.rules grants them /dev/i2c-* and /dev/gpiochip* access.
  * BEHAVIOUR CHANGE: the daemon and all five bridges now run with imud
    as their primary group, and the stream socket, /run/imud and
    /var/lib/imud are imud:imud. Only imud.service keeps gpio and i2c, as
    supplementary groups. Anything that read the stream socket or ran
    imud-status by virtue of gpio membership must join imud instead --
    on Pi OS that includes the default user: sudo adduser "$USER" imud.
  * Fix: config/imud-mqtt.conf (broker password) and
    config/imud-influxdb.conf (API token) installed mode 0644 despite
    carrying a SECURITY comment telling the operator to restrict them.
    Both are now 0640 root:imud, on upgrade over an existing 0644 file as
    well as on first install. The templates under
    /usr/share/doc/*/examples/ stay 0644.
  * Fix: a config that exists but cannot be read fell through to
    defaults, so a bridge exited reporting "disabled in config" for what
    was a permissions problem. EACCES is now distinct from ENOENT and
    fatal in the daemon and the bridges; a missing file still means
    defaults.
  * Fix: one configured ODR now means one rate everywhere. The filter was
    tuned from a round-to-nearest rate while every register-table driver
    programs the chip rounding up, so [imu] odr_hz = 900 tuned the MEKF
    for 833 Hz while the ISM330DHCX ran at 1660. The magnetometer rate
    was never snapped at all: [mag] odr_hz = 137 sized Rm for 137 Hz
    while the MMC5983MA sampled at 200. The driver is now the authority
    and the resolved rate reaches the chip, the filter and imud-imutest
    alike; startup logs "N Hz requested, M Hz actual" when they differ.
    Numerically unchanged with the shipped defaults, which are both
    already on their grids.
  * BEHAVIOUR CHANGE: odr_hz and the seven rate_hz keys now reject 0 and
    negative values. [mag] odr_hz = 0 gave Rm = 0 and a magnetometer
    Kalman gain of exactly 1. A config setting any of them non-positive
    started before and now refuses to start.
  * Fix: a client that connected to imud-prometheus and sent nothing
    stalled the bridge's single loop, stream reader included, for 2 s per
    iteration. The request is now read incrementally inside the loop's
    own poll and a quiet client is dropped at its deadline.
  * Fix: imud-imutest wrote its report to a file named "imud-im" when
    --report was not given.
  * Harden: tier-2 systemd sandboxing on all six units (empty
    CapabilityBoundingSet, @system-service syscall allow-list,
    RestrictNamespaces, LockPersonality, RestrictSUIDSGID,
    ProtectHostname, ProtectProc=invisible, ProcSubset=pid, UMask).
    imud.service deliberately omits ProtectClock= so the startup
    CLOCK_TAI check can call adjtimex(2) read-only. The AF_UNIX sockets
    are now born 0660 instead of being created wide and chmod'd after.
  * Harden: every fd is close-on-exec. Accepted sockets were the gap --
    POSIX does not carry FD_CLOEXEC across accept(), so NMEA/high-rate
    TCP clients, stream subscribers and imud-status connections were all
    inheritable on Linux.
  * Packaging: Architecture is now the little-endian release
    architectures (amd64 arm64 armel armhf i386 ppc64el riscv64) rather
    than any. The wire packet is a packed struct sent without byte-order
    conversion and the source #errors on a big-endian host, so any
    promised a build that cannot succeed.
  * Fix: make install DESTDIR=<root> captured config/cal.json into the
    packaging root when the build tree happened to have one, which made
    dpkg-buildpackage fail under dh_missing. A staged install now skips
    it; a real install is unchanged.
  * No wire-format change: the packet stays v17 / 276 bytes.

 -- Richard Simpson <richcreations@gmail.com>  Mon, 03 Aug 2026 12:00:00 -0700

imud (1.8-1) unstable; urgency=medium

  * New tool imud-imutest, shipped in the imud-utils binary package:
    validates a sensor driver against real hardware and writes a Markdown
    report to attach to an issue. imud-utils therefore gains a libgpiod
    dependency, since unlike imud-mon it talks to the sensor over I2C.
  * New drivers mpu9250 / mpu9255 (IMU) and ak8963 (magnetometer) for the
    TDK InvenSense MPU-925x 9-axis breakouts; all three experimental.
  * Fix: the IMU chip timer's declared period was trusted rather than
    measured. The reference ISM330DHCX runs 4.08% fast, so the wire's
    ts_wall_ns / ts_tai_ns ran roughly 2.4 s ahead over each 60 s anchor
    epoch and then stepped back, and every per-sample dt reaching the
    fusion filter was 4% long. The timestamp anchor now measures the
    counter's real period against CLOCK_MONOTONIC.
  * Fix: imud-cal accel printed the wrong physical instructions for all
    six positions and then silently fitted an identity scale. Re-run it.
  * Fix: imud-cal mag inflated the per-axis half-range by the hard-iron
    offset, biasing the soft-iron correction. Re-run it.
  * Files created by the daemon and tools now carry an explicit 0644
    instead of 0666 narrowed by the invoking process's umask.
  * No wire-format change: the packet stays v17 / 276 bytes.

 -- Richard Simpson <richcreations@gmail.com>  Fri, 31 Jul 2026 22:00:00 -0700

imud (1.7-1) unstable; urgency=medium

  * WIRE FORMAT v17 (packet 260 -> 276 bytes): four new MEKF diagnostic
    fields — innov_weight, innov_reject, nis_accel, nis_mag. The daemon,
    all five bridges and imud-mon validate the wire version and must be
    upgraded together; libimud consumers do not need recompiling.
  * Fusion: the MEKF error state grows 6 -> 9 with a Gauss-Markov
    wave-acceleration state, so the seaway's disturbance of the gravity
    measurement is modelled instead of treated as white sensor noise.
    New [fusion] keys mekf_wave_accel (0.8 m/s^2) and
    mekf_wave_accel_tau_s (0.5 s); either at 0 restores the 6-state
    filter exactly. New mekf_mag_dip_sigma_deg (1.0) admits the magnetic
    reference's dip uncertainty into the covariance in 3-D vector fusion.
  * Fusion: gross-outlier rejection widens 9x -> 25x the chi-squared
    gate, and the error-state reset Jacobian is applied to the covariance.
  * Fix: the innovation-covariance singularity test used an absolute
    determinant threshold on a matrix carrying physical units, silently
    skipping the accelerometer update once the filter converged.
  * Fix: initial attitude alignment averaged a hardcoded ~1 s of data, so
    a daemon started underway aligned to an arbitrary point in the wave
    cycle. The window is now [calibration] align_window_sec, default 5 s.
  * Fix: the update-gate health metrics used a per-sample averaging gain,
    giving a ~10 min time constant instead of 30 s; it is now time-based.
  * Config: the four MEKF noise densities are rejected at load if zero or
    negative; [mount] preset typos are fatal rather than silently applying
    a wrong rotation; new [mount] rotation_matrix key (9 row-major values,
    validated against R'R = I and det(R) = +1).
  * Engine-vibration detector: sample-rate-derived EMA time constant and
    threshold hysteresis so a boundary signal no longer chatters.
  * New imud-cal fit-ra mode reports the gravity-direction residual, its
    correlation time, the innovation distribution and the mean NIS from a
    rough-water capture.
  * imud-signalk: dest_addr now resolves hostnames and IPv6 via
    getaddrinfo(3) like every other bridge; unit gains AF_INET6.
  * imud-influxdb and imud-prometheus expose the four new diagnostics.
  * New test_fit_ra and test_mount suites; the wave benchmark averages
    over 12 seeds and the packet fuzz seed is generated from
    packet_build() with test_packet asserting it matches the wire.

 -- Richard Simpson <richcreations@gmail.com>  Mon, 27 Jul 2026 12:00:00 -0700

imud (1.6.2-1) unstable; urgency=medium

  * Internal dedup refactor, no functional change: the five bridges now
    share their startup/reload/main-loop scaffolding (src/bridge.c +
    src/sdnotify.c), all I2C drivers share the register-access helpers
    (src/drivers/i2c_io.h), and the WMM declination/field-reference
    derivation is a single shared function. Wire format, config keys,
    CLI, and log output are unchanged.
  * New test_bridge suite covers the shared bridge scaffolding.

 -- Richard Simpson <richcreations@gmail.com>  Mon, 20 Jul 2026 12:00:00 -0700

imud (1.6.1-1) unstable; urgency=medium

  * Bridges: separate the daemon-level `enabled` from per-output enables.
    Stock configs now ship the daemon enabled but every output off
    (imud-signalk udp_enabled; imud-influxdb udp_enabled/http_enabled;
    imud-mqtt broker_enabled; imud-prometheus http_enabled; imud-mavlink
    unchanged). The daemon runs and stays healthy under systemd until an
    output is turned on.
  * BEHAVIOR CHANGE: imud-signalk UDP output is now gated by udp_enabled
    (previously always on when the bridge ran); add udp_enabled = true to
    existing configs to keep sending UDP.
  * imud-influxdb: `transport = "udp"|"http"` is deprecated in favour of
    udp_enabled/http_enabled (both may run at once); the old key is still
    honoured with a warning.
  * Fix: a bridge with enabled = false no longer crash-loops under systemd
    (Type=notify) — it signals readiness before exiting cleanly.

 -- Richard Simpson <richcreations@gmail.com>  Mon, 20 Jul 2026 12:00:00 -0700

imud (1.6-1) unstable; urgency=medium

  * TCP outputs for NMEA, the framed binary stream, Signal K, and MAVLink
    (per-output tcp_enabled/tcp_bind_addr/tcp_port keys); libimud gains
    imud_connect_tcp (additive, SONAME unchanged).
  * BEHAVIOR CHANGE: built-in defaults now enable only the local AF_UNIX
    stream socket (NMEA UDP default flipped to off); set [nmea]
    enabled = true to restore the old posture.

 -- Richard Simpson <richcreations@gmail.com>  Sun, 19 Jul 2026 12:00:00 -0700

imud (1.5.1-1) unstable; urgency=medium

  * Fix C11 data races in config hot-reload and stream shutdown; validate
    gpsd/Signal K numeric input; log pthread errors via the return code.
    (ThreadSanitizer coverage added.)

 -- Richard Simpson <richcreations@gmail.com>  Wed, 15 Jul 2026 12:00:00 -0700

imud (1.5-1) unstable; urgency=medium

  * Initial multi-binary Debian packaging: imud, imud-utils, libimud0,
    libimud-dev, the five bridge daemons, and imud-wmm-data, built from
    the upstream Makefile install targets.

 -- Richard Simpson <richcreations@gmail.com>  Sun, 12 Jul 2026 00:00:00 -0700
