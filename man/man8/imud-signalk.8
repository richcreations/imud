.\" GENERATED from `imud-signalk --help` and man/h2m/imud-signalk.8.h2m — do not edit.
.\" Regenerate with `make docs-man`.
.TH IMUD\-SIGNALK 8 "2026-09-06" "imud 1.10.1" "System Manager's Manual"
.SH NAME
imud\-signalk \- bridge imud's binary stream to Signal K over UDP or TCP
.SH SYNOPSIS
.B imud-signalk
\&[\fB\-\-config\fR \fIPATH\fR]
\&[\fB\-\-version\fR]
.SH DESCRIPTION
.B imud-signalk
connects to the local AF_UNIX binary subscription stream published by
.BR imud (8)
and emits Signal K
.I delta
messages (JSON) over UDP for every value imud produces that has a standard
Signal K path.  Signal K did not reliably parse all of imud's NMEA 0183 output;
this bridge feeds Signal K natively instead, while the NMEA output is left
unchanged.
.PP
The bridge holds no hardware.  It reconnects automatically if
.BR imud (8)
restarts.  It reads the stream socket named by its own
.B socket
key (default
.IR /run/imud/imud-stream.sock ),
so
.BR imud (8)
must have its
.B [stream]
output enabled.
.PP
All values use Signal K SI units (radians, rad/s, metres).  The fields emitted
are:
.PP
.RS 4
.TS
l l.
navigation.headingMagnetic	always
navigation.headingTrue	when declination is known
navigation.magneticVariation	when declination is known
navigation.rateOfTurn	always
navigation.attitude	roll, pitch, yaw (always)
environment.heave	publish_heave, once heave settles
.TE
.RE
.PP
One delta is sent per UDP datagram at the configured rate (default 10 Hz),
timestamped from the packet's wall-clock time.  Configure a matching UDP
connection on the Signal K server (Server \(-> Connections \(-> Add, type UDP)
listening on the bridge's destination port.
.PP
With
.B tcp_enabled
the bridge additionally serves the same deltas newline-framed on a TCP
listener (default port 10113); configure a TCP client data connection on the
Signal K server instead \(em useful when the server runs on another machine
and \(dqjust connecting\(dq beats configuring UDP paths.  See
.BR imud-signalk.conf (5).
.IP
Signal K bridge: reads imud's stream socket and emits Signal K
delta JSON over UDP. Configured by [imud\-signalk] in its own file.
.SH OPTIONS
.TP
\fB\-\-config\fR PATH
Config file (default: \fI\,/etc/imud/imud\-signalk.conf\/\fP)
.TP
\fB\-\-version\fR
Print version and exit
.TP
\fB\-h\fR, \fB\-\-help\fR
Print this help and exit
.SH SIGNALS
.TP
.B SIGHUP
Reload the configuration.  The emit rate, destination address/port, source
label, heave publishing, and log level are applied live; if the reload fails to
parse, the running configuration is kept.
.TP
.BR SIGTERM ", " SIGINT
Graceful shutdown.
.SH CONFIGURATION
Read from
.I /etc/imud/imud-signalk.conf
(the
.B [imud-signalk]
section); the keys are documented in
.BR imud-signalk.conf (5).
.SH FILES
.TP
.I /etc/imud/imud-signalk.conf
Default configuration file.
.SH EXAMPLES
Run in the foreground against a local config:
.PP
.RS 4
.EX
imud-signalk --config /etc/imud/imud-signalk.conf
.EE
.RE
.PP
Enable as a service (after enabling imud's
.B [stream]
output and at least one bridge output \(em
.B udp_enabled
and/or
.B tcp_enabled
\(em in
.IR imud-signalk.conf ;
the daemon itself is enabled by default):
.PP
.RS 4
.EX
sudo systemctl enable --now imud-signalk
.EE
.RE
.SH AUTHOR
Richard Simpson
.SH "SEE ALSO"
.BR imud-signalk.conf (5),
.BR imud (8),
.BR imud.conf (5),
.BR imud-mon (1),
.BR imud-status (1)
.PP
The full documentation for
.B imud\-signalk
is maintained as a Texinfo manual.  If the
.B info
and
.B imud\-signalk
programs are properly installed at your site, the command
.IP
.B info imud
.PP
should give you access to the complete manual.
