imud-mqtt (1.10.0) unstable; urgency=medium

  * The bridges say when a heading is not referenced to north, each in its
    protocol's own terms rather than by all withholding it.  imud-signalk
    drops navigation.headingMagnetic/True/magneticVariation; imud-mqtt
    drops the matching topics, which are retained, and adds
    navigation/headingReferenced. imud-influxdb keeps publishing heading
    with heading_ref and mag_absent fields, and imud-prometheus keeps its
    gauge and adds imud_mag_uncal and imud_mag_absent. imud-mavlink gains
    SYS_STATUS, whose 3D_MAG present and health bits separate a board with
    no magnetometer from one that has stopped answering.
  * The armhf packages are now built on Raspbian at the ARMv6 baseline, so
    they run on every Raspberry Pi that boots a 32-bit userland.
    Previously they were built to Debian's ARMv7 baseline.  On a Pi 1 or
    Pi Zero/Zero W the previous packages installed without complaint and
    then died on an illegal instruction, because both ports report the
    same "armhf" architecture. Those boards can now install from the apt
    repository. The arm64 packages are unchanged.

 -- Richard Simpson <richcreations@gmail.com>  Sat, 05 Sep 2026 09:31:46 -0700

imud-mqtt (1.8.2) unstable; urgency=medium

  * BEHAVIOUR CHANGE: a bad value in imud-mqtt.conf is now fatal, where
    it was previously accepted with no message at any log level. A value
    too long for its field — broker host, topic prefix, username,
    password or the stream socket path — was truncated and used, so the
    bridge published to a topic, or authenticated with a password, that
    the operator never wrote. An out-of-range integer wrapped: a
    broker_port large enough to saturate strtol became (uint16_t)-1,
    which is port 65535. And nan, inf and 1e999 loaded cleanly into any
    float key. broker_port is also now bounded to 1..65535, because
    70000 fits an int perfectly well and then becomes port 4464 the
    moment htons() truncates it.
  * The daemon no longer leaves the binary stream socket bound with
    nothing behind it, so a bridge that connects is either served or
    refused. Previously a daemon whose stream thread failed to start
    still had the socket bound: the bridge connected successfully, into
    the backlog, and then waited forever with no diagnostic at either
    end. See the imud changelog.
  * No wire-format change: the packet stays v17 / 276 bytes.

 -- Richard Simpson <richcreations@gmail.com>  Wed, 05 Aug 2026 12:00:00 -0700

imud-mqtt (1.8.1) unstable; urgency=medium

  * Fix: /etc/imud/imud-mqtt.conf carries a broker password and installed
    mode 0644, so every local user could read it — despite the SECURITY
    comment in the file telling the operator to restrict it. It is now
    0640 root:imud, on upgrade over an existing 0644 file as well as on
    first install. The pristine template under
    /usr/share/doc/imud-mqtt/examples/ stays 0644.
  * Fix: a config file that exists but cannot be read is now fatal. It
    previously fell through to defaults, and since enabled defaults to
    false the bridge exited reporting "disabled in config
    ([imud-mqtt] enabled = false)" — naming the wrong cause entirely for
    what is a permissions problem, and the 0640 change above makes that
    the failure an operator is most likely to hit.
  * BEHAVIOUR CHANGE: the bridge now runs with imud as its primary group
    and no longer belongs to gpio. It carried Group=gpio only to reach
    the stream socket — it touches no hardware — and the socket is now
    imud:imud. The bridge connects as the socket's owner either way, so
    this needs no coordinated upgrade. It does mean the bridge can read
    its own 0640 root:imud config, which under Group=gpio it could not.
  * Harden: tier-2 systemd sandboxing (empty CapabilityBoundingSet, a
    @system-service syscall allow-list, RestrictNamespaces,
    LockPersonality, RestrictSUIDSGID, ProtectHostname, ProtectClock,
    ProtectProc=invisible, ProcSubset=pid, UMask=0077), and every fd is
    close-on-exec.
  * Documented: keepalive_s reaches the manual's key table, and
    username/password reach the config template.
  * Requires wire format v17 (packet 276 bytes), unchanged since 1.7.
    Topics, units, Home Assistant discovery, config keys and CLI are
    unchanged.

 -- Richard Simpson <richcreations@gmail.com>  Mon, 03 Aug 2026 12:00:00 -0700

imud-mqtt (1.7) unstable; urgency=medium

  * Rebuilt for wire format v17 (packet 260 -> 276 bytes); must be
    upgraded together with the daemon. Topics, units, Home Assistant
    discovery, config keys and CLI are unchanged.

 -- Richard Simpson <richcreations@gmail.com>  Mon, 27 Jul 2026 12:00:00 -0700

imud-mqtt (1.6.2) unstable; urgency=medium

  * Rebuilt on the shared bridge scaffolding (internal refactor); no
    functional change — output, config keys, CLI, and log text are
    unchanged.

 -- Richard Simpson <richcreations@gmail.com>  Mon, 20 Jul 2026 12:00:00 -0700

imud-mqtt (1.6.1) unstable; urgency=medium

  * New broker_enabled key gates the broker connection and publishing;
    keeps a fresh install from repeatedly dialing an unconfigured broker.
    The stock config ships the daemon enabled with the output off.
  * Fix: enabled = false no longer crash-loops under systemd (Type=notify)
    — the bridge signals readiness before exiting cleanly.

 -- Richard Simpson <richcreations@gmail.com>  Mon, 20 Jul 2026 12:00:00 -0700

imud-mqtt (1.5) unstable; urgency=medium

  * The temperature topic now reports the real IMU die temperature (the
    daemon previously always published a 25.0 degC placeholder).
  * SIGHUP-reloadable fields shared with the mosquitto network thread are
    now C11 _Atomic.

 -- Richard Simpson <richcreations@gmail.com>  Wed, 15 Jul 2026 00:00:00 -0700

imud-mqtt (1.4) unstable; urgency=medium

  * New sea-state topics under environment/ (waveHeight, wavePeriod,
    rollPeriod, rollAmplitude, pitchPeriod, pitchAmplitude), riding
    publish_heave and gated on the packet's WAVE_VALID flag.
  * New engine/running ON/OFF topic with Home Assistant binary_sensor
    discovery (device_class: running).
  * Rebuilt against wire v14.

 -- Richard Simpson <richcreations@gmail.com>  Sat, 11 Jul 2026 11:35:35 -0700

imud-mqtt (1.3) unstable; urgency=medium

  * Link libimud for stream access and packet validation (drops the local
    copy of the socket code). Runtime dependency: libimud.

 -- Richard Simpson <richcreations@gmail.com>  Fri, 10 Jul 2026 12:00:00 -0700

imud-mqtt (1.2) unstable; urgency=medium

  * Rebuild against imud packet v12 (228 bytes).
  * Add an environment/heaveRate topic (m/s) with Home Assistant discovery.
  * Gate heave/heaveRate on the new HEAVE_VALID flag: their state is withheld
    until the heave estimator settles (~10·τ), so subscribers never see the
    startup transient. HA discovery is still advertised on publish_heave alone,
    so the entity exists and reads "unavailable" until then.

 -- Richard Simpson <richcreations@gmail.com>  Fri, 10 Jul 2026 03:00:00 -0700

imud-mqtt (1.1) unstable; urgency=medium

  * Initial release: bridge imud's binary stream to MQTT — scalar telemetry
    topics plus Home Assistant MQTT discovery, via libmosquitto.

 -- Richard Simpson <richcreations@gmail.com>  Fri, 10 Jul 2026 01:32:55 -0700
